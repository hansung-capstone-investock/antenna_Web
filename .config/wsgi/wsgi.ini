[uwsgi]
chdir = /root/antenna_web/
module = antenna_web.wsgi:application
home = /root/myvenv/

uid = root
gid = root

http = :8000

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/antenna_web.pid
logto = /root/log/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true
